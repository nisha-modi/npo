## NPO

In one terminal:

```bash
cd frontend
npm install
npm start
```

In another terminal:

```bash
npm install -g azure-functions-core-tools
cd server
npm install
npm start
```

Open a browser to `http://localhost:4200`
